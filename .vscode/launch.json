{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch tgforwarder with actual config",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/tgforwarder/main.go",
            "args": [
                "-config",
                "${workspaceFolder}/config.yaml"
            ]
        }
    ]
}